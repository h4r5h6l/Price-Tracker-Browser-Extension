# Price-Tracker-Browser-Extension

pip install scrapling[all]

pip install requests beautifulsoup4 lxml fake-useragent
requests — makes HTTP calls
beautifulsoup4 — parses HTML
lxml — fast parser backend
fake-useragent — rotates browser identity strings


cd src && python -m app.crawling.webpage.crawl_amazon_products